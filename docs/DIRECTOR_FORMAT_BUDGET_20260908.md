# 导演格式与预算调整

2026-09-08：源码已修改，255 项相关回归通过。未发起付费 AI 验收。

- 有效多句 ID 按原顺序展开，保持章节、原句选择与商品证据。每句时长取自字幕；重复 ID 留给既有内容审计，不静默删除。空值、非法 ID、未知 ID 和用户内容禁选 ID 不通过格式接收。商品冲突仍走原有商品审计。
- 两轮 AI 的调用数量、模型及思考设置保持原配置；正式一次选片仍不追加时长纠偏调用。
- 选片合同增加程序计算的章节累计预算 budget_end，保留单章预算与全片原声目标，合并重复预算指令。强调章内完整意思、预算间调剂和删减重复证明由 AI 完成。
- 故事输出移除重复结构理由与微型叙事标签，传给选片的合同不再重复 buyer_advance。保留原故事职责、全部完成要求和商品边界。
- 选片不再返回自报总秒数与重复连读关系；程序实测时间，保留必要语义依赖、章节证据回执和内容缺口。

离线回放见 artifacts/director_format_replay_20260908.json：失败样本 20 个单元中的 10 个多句组展开后为 40 句；三份样本的章节与 ID 顺序一致。此回放仅验证格式与顺序，不代表重新选片后时长或内容已达标。

待验收：同素材首次成功率、成片时长偏差、讲解完整顺滑程度、每条合格结果总费用。当前无新模型实测，不能宣称时长控制和费用已经改善。
