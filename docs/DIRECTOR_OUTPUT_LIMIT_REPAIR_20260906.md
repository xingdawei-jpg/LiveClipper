# AI 导演输出截断修复（2026-09-06）

## 故障依据

- 三方案任务 `smart-cut-1788651130-a8dca8`：第二次选片请求触及 32,000 tokens 上限，JSON 不完整；总计 84,863 tokens，失败仍产生费用。
- 后续单方案任务 `smart-cut-1788651590-f94f9c`、`smart-cut-1788651815-7da340`：选片阶段同样触及原 16,000 tokens 上限。
- 旧账本只有输出总量，未保存推理用量与正文用量，无法追溯断在第几套方案或精确拆分。不能据此断言都是正文过长，或都是推理消耗。
- 此前成功的三方案任务 `smart-cut-1788650186-9ecb69` 只代表一次时长达标，不能证明原来的输出余量可靠。

## 修复范围

- DeepSeek V4 选片保持低强度推理，单/双/三方案分别提供 32,768 / 48,384 / 64,384 tokens 的推理与正文共用额度，并留出相应等待时间。故事阶段保持原额度和非推理模式。
- 第二次回复不再重复整章口播全文；AI 在同次推理内核对章节连读与时长，返回最终短句 ID、商品归属和简短核对结果。程序按原字幕 ID 重建展示全文，不改写、不选句、不后补。
- 正常流程仍是故事一次、选片一次；未新增自动付费重试或第三次时长补齐请求。仍使用配置的 Flash。
- 账本新增输出上限、停止原因、推理 tokens、非推理输出 tokens，以及两类文本的字符数。推理已包含在输出总量内，不重复计费；供应商未给出的分项保留未知。账本不保存提示词、台词正文或推理文本。
- 失败预览也生成会话用量报告并挂入预览数据，避免失败记录只有报错、缺少费用依据。

接口字段依据：[DeepSeek Chat Completion 官方文档](https://api-docs.deepseek.com/api/create-chat-completion/)。提高上限不等于实际消耗增加到该上限，但允许模型输出更多，因此耗时和费用必须实测。

## 验证

最终 222 项相关回归检查通过，覆盖选片额度、保持两次调用、原字幕全文恢复、时长控制、候选池、预览路由、词编辑、用量分项，以及下面的有限格式恢复。

真实预览验证使用同一卫衣和休闲裤套装素材，目标 60 秒，1.15 倍速，按当前容差交付范围为 50–70 秒。衣服和裤子均可讲，未新增单品限制。

第一轮三方案 `smart-cut-1788668412-7c081b` 完整返回，80,801 tokens，246.99 秒，约 0.196152 元。选片阶段输出 28,315 tokens，其中推理 19,172、正文 9,143，停止原因 `stop`。三个方案的实际时长为 60.626、47.217、41.896 秒；后两套偏短，不能算时长验收通过。所有原始 ID 和顺序均保留，没有程序丢句。

单方案 `smart-cut-1788668707-fe1b4e` 完整返回，67,507 tokens，229.60 秒，约 0.140642 元，26 句原声 68.995 秒，预计成片 59.996 秒。选片输出 23,857 tokens（推理 20,005、正文 3,852），已超过旧 16,000 上限，但在新 32,768 额度内正常结束。原始顺序完全保留，没有后补或第三次请求。

第一轮三方案的偏短原因：AI 将部分方向限制在颜色、剪裁等单个侧重，认为缺少该侧重的更多独立证据便提前结束，并把每章预算改写为较小的已选时长。因此进一步澄清多方案是不同购买切入点和前段说服路径；每套都需支撑完整目标时长，必要证据允许跨成片复用，不能为了版本差异缩短成片。同章的机制、体验、条件和搭配结果仍须讲充分。禁止将较小的已选秒数反写为预算，但真正缺少合格素材时仍如实报告。

这项补充仅改变 AI 本次规划、选片的要求，不增加调用次数或程序语义补齐；随后 90 项针对性检查通过。

## 最终三方案与已付费回复恢复

`smart-cut-1788669004-6f4808` 在新要求下完成两次真实 Flash 调用，共 77,832 tokens，约 0.192226 元。选片阶段输出 28,919 tokens，其中推理 17,394、正文 11,525，停止原因 `stop`，距 64,384 上限仍有 35,465 tokens 余量。约 3 分 39 秒返回，未截断。

这次出现了另一个可追溯的格式错误：原始选片 JSON 第 854 行的 `continuity_links.relation` 说明漏了末尾一个双引号。供应商请求成功不等于应用解析成功；原任务因解析失败停止，其 `failure.json` 已包含完整用量报告。

新增有限修复：仅在 `relation` 说明的字符串缺少行末引号、且下一行明确结束该对象时补引号。没有改说明文字、字幕 ID、顺序或时间；不修复缺失 ID、缺失容器或截断内容。已通过正常 JSON 不变、中文与转义引号、CRLF、拒绝不明确/截断内容等检查。该真实回复只增加了 **1 个字符**。

为避免再次收费，用原故事和原选片回复重放正式预览 worker。两阶段的输入指纹及模型名称与原付费账本逐一一致；恢复过程中没有新增 AI 账本调用。一次性恢复后已退出回复替换作用域，当前页面之后的新预览仍正常请求 DeepSeek。

恢复任务 `smart-cut-1788669590-6f708f`，主预览 `679bf81d6e864a7ab893fac88f62cb15`，本地重放用时 35.82 秒，无新增 AI 费用。当前源码服务监听 8765，PID `34276`；为保留本次内存预览，通过 `workspace/recover_director_preview_20260906.py` 恢复后运行正常 `server.main()`。未打包或发布。

| 方案 | 原始 AI / 预览句数 | 原声实测 | 1.15 倍速后预计成片 | ID 与顺序保真 |
|---|---:|---:|---:|---|
| S1 | 31 / 31 | 68.760s | 59.791s | 完全一致 |
| S2 | 31 / 31 | 69.570s | 60.496s | 完全一致 |
| S3 | 22 / 22 | 60.279s | 52.417s | 完全一致 |

全部在当前 50–70 秒容差内，无无效 ID、无重复 ID、无后补、无第三次 AI 请求。第三套仍偏向下限，不能声称三套都接近 60 秒，也不能据一次恢复后的成功声称跨素材稳定。

本轮三次真实测试合计 **6 次 AI 请求、226,140 tokens，约 0.529020 元**，包含中途因格式失败的第三次测试；免费本地恢复不重复计费。最终报告中的 77,832 tokens / 0.192226 元是这套三方案的原付费用量，不是 35.82 秒内新产生的调用。

最终证据：`final_three_plan/final_acceptance.json`、`recovery_start.json`、`recovered_casting.json`、`final_runtime.json`、`final_source_hashes.json`。原始错误回复与 `failure.json` 保留在原任务目录，没有覆写。

证据目录：`workspace/director_output_limit_20260906/`。费用按本次工作台非峰时 Flash 费率估算：每百万输入 1.5 元、缓存输入 0.05 元、输出 4.5 元；以供应商实际账单为准。

本次验收范围是正式预览与源码运行；不等同于 MP4 导出、视听质量或跨素材稳定性验收。
