# 最新混剪审计：2026-09-07 08:08

预览任务：`mix-1788739357-9d45cf`。预览 ID：`aef4360a47b34de9a67225465597a31c`。
对应导出任务：`mix-1788741803-c42ff9`。

结论：最终成片实测89.443秒，90秒目标已达到，双素材链路确实生效。预览花费约¥0.141；结合导出字幕阶段的额外调用，整次成片估算约¥0.160。当前主要问题是字幕修复空耗、劝退观众的口播、重复和未完成的章节承接。不能仅因程序返回complete/pass就认定内容质量通过。

## 用量、金额与等待时间

| 阶段 | 输入 tokens | 其中缓存 | 输出 tokens | 其中推理 | 谷时估算 | 请求耗时 |
|---|---:|---:|---:|---:|---:|---:|
| 故事 | 11,503 | 640 | 2,081 | 未提供明细 | ¥0.025691 | 13.923秒 |
| 选片 | 11,151 | 0 | 21,851 | 19,024 | ¥0.115056 | 151.670秒 |
| 合计 | 22,654 | 640 | 23,932 | 已知19,024 | ¥0.140747 | 165.593秒 |

- 预览用量46,586 tokens；2次成功调用，0次重试、0次时长补充AI；单方案。未生成三套完整方案。
- 两次请求开始于北京时间08:08，均属谷时。按官方价格，缓存输入0.05元/百万、未缓存输入1.5元/百万、输出4.5元/百万。来源：https://api-docs.deepseek.com/zh-cn/quick_start/pricing/
- 选片内部推理估算¥0.085608，占本次总费用60.82%。输出21,851，低于32,768上限，正常stop。
- 预览任务总耗时522.167秒（8分42秒），两次DeepSeek请求合计约2分46秒，约5分56秒在这两次请求之外；本次审计没有将这些时间进一步归因到具体处理阶段。
- 本次为不同素材的90秒混剪，不能与此前part_002/003的60秒单素材预览直接作降本A/B结论。

### 导出阶段另有一次无效字幕修复

- 北京时间08:58:39.706，账本新增`subtitle_processing`调用，request_id为`08d3c561334641809f75bddee7cfb0f9`。输入894 tokens（缓存256），输出4,000 tokens全部为推理，正文0字符，finish_reason为length。谷时估算¥0.0189698。
- 该条账本的task_id、session_id和request_started_at均为空，不能凭任务编号直接追溯。它发生于本次导出日志08:58:07本地识别完成与08:58:39开始字幕烧录之间；当时任务列表中只有此导出在运行，模型和处理路径一致。因此将其归属本次导出是时间线与代码路径的交叉推断，而不是任务ID已关联的证明。
- 预览与该字幕调用合计51,480 tokens、估算¥0.1597168，约¥0.160。界面的“本次预览”费用只覆盖前两次导演调用，不能代表从预览到成片的完整费用。
- 当前`app/cutter_logic.py`的字幕请求固定max_tokens=4000，没有关闭思考；记账在读取、验证正文前就将success写为true。实际响应已证明推理占满输出额度，未产生修复正文。空正文不能通过逐行对应检查，代码回退原始ASR字幕；本次最终画面仍可见字幕问题。仅HTTP成功不等于字幕修复成功。
- 优先处理字幕修复的思考模式、length/空正文判定和任务归属，不需要再增加一轮时长AI。本次审计未实施这些修改。

## 时长与素材使用

- 用户目标90秒，导出1.15倍速，原声目标103.5秒。
- 离线按真实安全库存、故事、控制参数重建选片请求，其指纹与已记录的实际调用完全一致：`ae0dab5fd16e06d32c80ba0c0819ca33fcab63eb576c1381ac902a18987fdc3c`。紧凑合同各章预算合计103.5秒，证明预算换算实际生效。
- AI最终选择37句，原声99.806秒，预计成片86.788秒，距90秒中心目标短3.212秒。位于当前75–105秒容差内；无重复ID和无效ID。
- 导出将选中的语句转换为精确裁剪范围，日志记录38段、102.9秒原声、预计89.5秒。最终MP4实测89.442993秒，视频流89.433333秒、音频流89.442993秒；相差约10毫秒。预览字幕条目时长与导出精确语句范围不是完全相同的计时口径，最终以MP4为准；不能把这约2.65秒差值解释成新增AI补片。
- 第一素材`Ayobe小贤202609061051_part_003.mp4`贡献29句、73.991秒原声；第二素材`Ayobe小贤202609061121_part_001.mp4`贡献8句、25.815秒原声。第二素材提供穿法和收尾，混剪不是只取第一条视频。
- 当前保存的37个选中条目与AI预览一致，没有新增或删除候选。导出拆成38个裁剪段，其中一个字幕条目有两个选中语句片段，不等于新增一次AI选片。
- 两素材均为1088×1920、22fps，有音轨。抽样画面中均展示同一件蓝绿色U领毛衣；未见明显混入裤子的独立卖点。第二素材尾部帽子已摘下，收尾有造型变化。此处仅为抽样视觉核对，不能代替连续听看成片。

## 具体内容问题

1. **C2把购买理由剪成劝退，优先级最高。** ID95/96、103/104/105、113合计15.271秒原声，预览预计占成片13.279秒，最终约位于9–23秒。最终留下“你直接离开我的直播间”“一定不是正常的路人会穿的”，缺乏完整解释，降低购买说服力；19.8秒画面已核实烧录了劝退字幕。原片ID100解释“摆脱路人感”，但因当前禁用词“现在”被硬过滤；ID98“从做工、版型上来说不挑人”仍可选却未选。故事依赖不可选的解释，选片未补足可选证据，却将本章标complete。
2. **开头重复而缺少立即兑现。** C1连续五句围绕“没有平替／你要相信／从没买过／独一份”重复表达颜色独特，约8.4秒成片。之后紧跟C2争论，直到约21.7秒才进入具体颜色信息。没有平替只是主播断言，不能单凭标签result视为已提供结果证据。
3. **C5材质章节未完成，而且留下未承接的话头。** 故事要求成分和舒适度证据，最终未选择具体成分，却保留“贴在婴儿身上都不会敏感”等绝对体验断言。ID154以“会扎的，对，但是……”开始，缺少ID153的对象说明。ID155/156共同闭合“配比非常合理”，但156又开启“我跟你们讲它的来头”，下一章直接切斜肩穿法。这里不能标整章完成。
4. **C6字幕存在含义冲突，需听音核对。** ID227文字为“小鞋肩穿是有问题”，与前后“完全做得到”“也可以”相反，疑似转写问题。本次未以听音证明主播究竟说了什么，不能直接将其改写成“没有问题”。斜肩和一字肩各有重复确认句。
5. **故事完成判定过宽。** 所有章节均标complete、整片标pass；但上述C2/C5的购买问题和必要衔接未完成。C3还保留了“克莱因蓝更显白一点”的比较，作为真实陈述本身无错，但未充分解释为何仍应选择当前颜色，削弱原故事承诺。

当前禁用词也排除了ID131“现在它的整个大小是刚刚好的”，导致领口章节少了直接的最终结果。这说明应先检查故事是否能被当前可选池执行，而不能继续将不可选上下文中的结论冻结为必讲要求。审计没有修改禁用词、字幕或片单。

## 导出与证据边界

- 导出任务已完成，开始08:43:23.307，结束08:59:34.628，任务耗时16分11秒；没有重做导演选片。额外字幕调用的归属和记账缺陷见上文。
- 导出耗时主要在本机处理。日志显示准备/画面分析后08:47:47开始裁剪，08:53:17进入拼接；拼接92.7秒，08:54:50–08:56:02完成整片画面处理，08:56:03–08:58:07完成本地字幕识别流程，08:58:40–08:59:33烧录字幕。不能将16分钟都算作DeepSeek等待时间。
- 硬件自检中NVIDIA/AMD编码器不可用，Intel QSV通过并被选中；这些自检失败没有使成片任务失败。逐片动态推拉日志显示使用OpenCV，观察到的单段处理速度约0.31–0.41倍实时，值得作为后续速度审计对象；不能只看GPU自检就将全部耗时归因于硬件问题。
- 最终文件：`C:\工作\output\xx\8.17\Ayobe小贤202609061051_part_003_混剪_20260907_084325.mp4`，1080×1920，H.264/AAC，168,274,759字节。
- 已验证最终MP4时长和音视频流，抽查8个成片画面及烧录字幕。画面主体仍是同一件蓝绿色毛衣，72.3秒有斜肩实拍，约80秒进入摘帽收尾。字幕使用当前设置的方正舒体、65%不透明度，在浅色毛衣上较细淡；这属于可读性问题，不是字幕未烧录。
- 原素材抽样图为`ai_selection_audit/mix-1788739357-9d45cf/source_contact.jpg`；成片抽样图为同目录`final_contact.jpg`，另保存`render_task_logs.json`和`final_media_probe.json`。本次未连续听看全片，疑似ASR错词、具体音节是否被切断和连续转场自然度仍不能视为已听音确认。
- 主要证据位于`workspace/ui_commerce_director_experiment/mix-1788739357-9d45cf/`的`ai_cost_report.json`、`source_info.json`、`*.director_story.response.txt`、`*.director_duration_control.response.txt`、`*.director_product_control.response.txt`，并与当前预览API返回的37句、草稿选中条目和原字幕交叉核对。
- 本次没有修改业务代码、重启服务或发起付费AI请求。
