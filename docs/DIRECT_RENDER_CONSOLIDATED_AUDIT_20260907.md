# 最新直接成片与历次审计问题清单

审计日期：2026-09-07。审计对象：`smart-cut-1788746911-4de05d`，用户点击智能直接成片，10:08:31开始，10:22:37完成。内部预览ID为`10c43cb035e44d7188b66821cb025348`；内部生成片单后自动导出，不需要用户先操作预览。

结论：本次直接成片链路和时长通过，内容主线比上一条混剪完整；字幕修复空耗、章节完成判定过宽、商品校验与成功提示不一致等问题仍在。不能将文件成功、AI自评pass或旧评分器的61分当作商业内容质量验收。

## 1. 本次实测

| 项目 | 结果 |
|---|---|
| 素材 | Ayobe小贤202609061221_part_002.mp4 |
| 主商品 | 带固定丝巾/挂脖、背部纽扣的假两件上衣；允许搭配商品 |
| 用户目标 | 60秒、1.15倍速、单方案 |
| 实际成品 | **58.652993秒**，1080×1920、30fps、H.264/AAC、104,003,687字节 |
| 音视频流时长 | 视频58.633333秒、音频58.652993秒，相差约20毫秒；不等于已证明全程口型同步 |
| 选片 | 25句，原字幕65.19秒，预览估算56.687秒；无重复ID、无无效ID |
| 导出范围 | 同样25句，同顺序同文本（忽略标点），起点全部相同，每句末尾多0.1秒，合计67.69秒原声 |
| AI调用 | 故事1次、选片1次，无导演重试、无第三次时长补选；另有字幕修复1次 |
| 顶层任务耗时 | **845.898秒，14分6秒**；仅导出阶段约514.4秒 |
| 运行环境 | 源码服务2026.9.6.1，PID21772，03:16:16启动；导演代码03:10:15修改，已在启动前落盘 |

最终文件：`C:\工作\output\xx\9.7\Ayobe小贤202609061221_part_002_20260907_101403.mp4`。

### 费用

| 环节 | 输入tokens | 其中缓存 | 输出tokens | 已知推理tokens | 总tokens | 峰时预估 |
|---|---:|---:|---:|---:|---:|---:|
| 故事 | 7,327 | 640 | 2,380 | 未提供明细 | 9,707 | ¥0.041545 |
| 选片 | 7,126 | 0 | 13,522 | 11,543 | 20,648 | ¥0.143076 |
| 字幕修复 | 706 | 256 | 4,000 | 4,000 | 4,706 | ¥0.0373756 |
| 合计 | 15,159 | 896 | 19,902 | 已知15,543 | **35,061** | **¥0.2219966，约¥0.222** |

导演两次请求合计30,355 tokens、约¥0.185。字幕账本缺task_id、session_id和request_started_at；其完成时间10:22:19.208，恰位于本任务10:21:45本地识别完成与10:22:19开始字幕烧录之间，且现存任务记录没有另一项同时运行。因此字幕费用归属为时间线与处理路径交叉推断，不能声称已由任务ID直接关联。

本次两次导演请求在周一10:11–10:13，字幕请求也在上午高峰内。官方Flash峰时价格为缓存输入0.10元/百万、未缓存输入3元/百万、输出9元/百万；北京时间周一至周五9–12点、14–18点为峰时，谷时为其一半。2026-09-07重新核对：[DeepSeek官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)。界面仍应只显示对应峰/谷时预估金额，无需显示上述计算明细。

## 2. 合并后的问题清单

P1表示优先修复，P2表示随后修复或优化；“历史待复测”不表示本次复现，也不表示相关代码尚未修改。

| 编号 | 优先级/状态 | 问题 | 本次及历史证据 | 修复或验收要求 |
|---|---|---|---|---|
| 01 | P1，连续复现 | **字幕修复消耗推理却没有正文** | 07:27、08:58、10:22三笔字幕回执均为length、输出4,000、推理4,000、正文0。本次空耗约¥0.037 | 字幕纠错显式控制思考模式，检查finish_reason、空正文和逐行对应；失败回退必须如实标明，不能只提高上限掩盖问题 |
| 02 | P1，连续复现 | **业务失败记成成功，完整费用追溯不全** | 字幕回执在验证正文前记success=true，缺任务归属；本次任务费用报告仅含30,355导演tokens。历史还发生成功重试报告漏掉首轮格式失败消耗 | 关联顶层任务、阶段、request_id和attempt；区分接口成功与业务成功；完整费用含失败和字幕调用，但保持“预览费用”的范围准确 |
| 03 | P1，连续复现 | **章节未完成却全部complete/pass** | 本次C4承诺具体裤装例子却没选小黑裤/西裤；C5承诺风衣或西装实拍，抽样只有上衣、帽子、手包；C7承诺定位工艺说明，选片仅有活动不掉。上次混剪C2/C5也有未完成却通过 | AI给出章节职责对应的实际证据和缺口；程序核对引用和状态一致性，不能仅照抄complete。保留AI语义选片权，不以关键词拼凑替代判断 |
| 04 | P1，历史问题仍待闭环 | **故事承诺与可执行素材脱节** | 之前60秒故事要求价格、99元比较和售后，但任务禁止；混剪的“摆脱路人感”“领口刚好”依赖被“现在”过滤的句子。本次没复现价格/售后泄漏，但仍有承诺比所选证据多的问题 | 故事可读全文理解商品，但必须区分可执行证据和仅供理解的上下文；不要冻结依赖不可选句子的必讲要求。不要通过取消用户禁项来解决 |
| 05 | P1，本次新增明确证据 | **商品关系判定、警告和最终成功提示互相矛盾** | product_control标conflict，ID12/15/16/77/78被认为邻近裤子；blocking=false，warnings提示“商品归属未核实”。但预览文案称“已核对商品归属”，最终任务warnings为空 | 先区分上衣的搭配/塞衣穿法与裤子的独立卖点，再使内部状态、对外提示和导出决策一致。本次这些话主要服务上衣，不应一见“裤子”就删，也不能把未核实说成已核实 |
| 06 | P2，连续复现 | **重复占时，挤掉有效信息** | 本次C3五句约11秒反复讲背面好看/背影杀手/女明星；C5四句约7.5秒讲风衣很哇塞、有腔调，却无外搭效果。上一条混剪开头重复独特颜色 | 同一次AI选片优先保留新增购买信息，用可用证据替换重复赞美；不自动后补，不增加一轮凑时长AI |
| 07 | P2，本次明确定位 | **旧评分器不适配导演章节，制造误报** | 实际片段类型是C1–C7；旧逻辑只认含hook的类型、close/cta/urgency收尾，并偏好5–8段，给25个短句扣分；得出61分、缺开头/收尾、品类C1。上次混剪也出现相同警告 | 导演片单采用适配章节和短句的评估；区分技术校验与内容评估。这些旧警告不能作为自动重选开头或强加CTA的理由 |
| 08 | P2，连续复现 | **速度瓶颈不只在DeepSeek** | 本次两次导演约120.22秒；源ASR约194秒，渲染准备到首段裁剪约130秒，动态推拉约86秒，最终本地ASR流程约73秒。整任务14分6秒 | 继续保留阶段耗时；优先定位本地识别启动/识别、画面分析和OpenCV推拉，按相同素材与设置比较。不能把总耗时都归因于AI或字幕烧录 |
| 09 | P2，降本尚未完成同素材验收 | **选片推理仍占大头，删提示词不保证总价降低** | 本次选片13,522输出中11,543是推理；仅这部分约占导演费用56%。此前60秒预览推理30,231、14,030；上一条混剪19,024 | 先消除故事和证据冲突，再以同一素材、同一故事、相同费率做成本与质量比较；降低上限可能截断结果。不同素材、时長及峰谷价格不能直接作降本A/B |
| 10 | P2，计时口径需统一说明 | **预览估算与导出精确范围不一致** | 本次预览56.687秒，实际58.653秒；25句各加0.1秒尾部。混剪预览86.788秒，实际89.443秒。旧运行报告duration=67.7是原声区间合计 | 统一区分原声、变速后估算、最终文件实测；尽早以导出相同边界预测。尾部差值不等于AI自动补片，保真状态也不等于所有音节已听审通过 |
| 11 | P2，连续视觉观察 | **字幕可读性和造型连续性需验收** | 52号方正舒体、65%不透明度，图案衣料上较细淡；镜像25%概率及动态推拉已启用，帽子/墨镜有跳变 | 基于用户设置评估成片，不能把这些效果一概当程序故障。字词纠错和对比度可读性分别验收；镜像、转场和音节边界需连续听看 |

### 本次最具体的内容缺口

以下时间为按导出范围累计、1.15倍速推算的章节位置，用于定位；最终编码时长比该累计值约短0.21秒，不是逐帧标注。

- **约31–39秒，裤装搭配章：** 原片ID13“你也可以搭家里的那种小黑裤”、ID14“西裤类型都行”都有，当前安全库存和1–8秒可选池均保留。最终只选ID12/15/16，留下不要抢镜、要极简，却漏掉已经存在的具体答案。这是选片取舍问题，不是素材缺失。
- **约39–46.5秒，风衣章：** 所选ID125/126/129/130是搭风衣的口头建议。41.5和44.5秒画面仍为单穿上衣配帽子、手包；原片ID127还在要求拿风衣来。不能把口头建议自动标成已完成风衣实拍示范。
- **约54–58.7秒，活动证明：** 有抬手动作与“不掉”的口播，能回扣前文固定设计，是本次有效的收尾价值。原片随后用披肩作对比，支持其商品指代；不要因抽样时主播戴墨镜就推断“不掉”讲的是墨镜。但所选两句没有完成故事另列的“定位工艺/立体度说明”。

## 3. 已改善与不可混淆的事项

1. **直接成片已跑通。** 单次点击自动完成故事、选片、渲染，不要求用户先完成手动预览；内部日志出现“预览”不等于走错用户入口。
2. **本次时长达到目标附近。** 58.65秒距60秒仅短1.35秒；上次混剪89.44秒也接近90秒。两个实例都没有第三次时长AI。不能再把这两条归为严重时长不足，也不能据此宣布所有素材、所有方案时长稳定。
3. **单方案确实只执行一套详细片单。** 故事返回一个完整主方案和两个方向卡，选片回复只有S1；没有偷偷生成三套完整选片。本次不覆盖用户点击二、三方案后的切换与计费行为。
4. **本次主线有实质改善。** 开头三句形成手残痛点→系不好→已经固定好的解决；中段背面有实际展示、塞衣有穿法示范，收尾有活动证明。上一条混剪“直接离开直播间”的劝退片段没有在本次复现。
5. **导演输出没有触顶。** 本次选片输出13,522，低于32,768并正常stop；当前复现的输出上限故障在字幕修复。不要把两者混为一谈。
6. **时间和费用要同口径比较。** 上次混剪预览加导出为51,480 tokens、谷时约¥0.160，本次直接成片为35,061 tokens、峰时约¥0.222。tokens更少而金额更高与峰谷价格有关；素材和目标时长也不同，不能据此认定优化有效或退步。
7. **NVIDIA/AMD自检失败不是本次任务失败。** 随后Intel QSV自检通过并使用，成品已完成。这两条探测没有消耗DeepSeek tokens，无需据此安装不匹配的显卡驱动。

## 4. 历史待复测，不算本次复现

- 三方案在90秒目标下曾输出90.7/67.5/72.4秒；最新两次单方案不能关闭多方案时长与完整多MP4验收项。
- 二、三方案点击后自动回主方案的问题，本次没有交互测试，不能写成仍存在或已彻底修复。
- 历史故事JSON内部引号错误、阶段重试、失败费用汇总问题，相关优化已有既往工作；本次没有触发导演重试，不能替代那次真实失败回放和重试归属验收。
- 多文件/多方案ASR复用需要对应场景验证。本次单素材、单成品不能证明多文件复用的净收益；也不应要求源识别与最终音频识别复用同一份字幕。
- 连续听音、口型、每处短句尾音及转场自然度，本次没有做完整人工听审。抽样画面和字幕只能支持限定范围的内容、画面审计。

## 5. 建议处理顺序

1. 字幕修复空耗与业务失败判定；补全任务费用归属。
2. 章节完成证据、故事与可执行库存一致性；修正商品关系误判及警告传递。
3. 清理旧评分误报和计时口径，再优化有测量证据的本机热点。
4. 最后做同素材降本对照，以及单/多方案的真实成片验收。正常导演仍保持两次调用，语义选片和顺序仍由AI决定。

## 6. 证据与范围

- 本次业务记录：`workspace/ui_commerce_director_experiment/smart-cut-1788746911-4de05d/`，包括故事、选片、时长、商品关系原始回复、source_info和ai_cost_report。
- 本次审计归档：`ai_selection_audit/smart-cut-1788746911-4de05d/`，保存任务与预览快照、任务日志、运行报告、费用回执、最终媒体探测、范围对比、章节时间表、当前可执行库存、12帧抽样图和58秒原尺寸画面。
- 历史对照：[混剪审计](MIX_DIRECTOR_AUDIT_20260907_0808.md)、[单方案费用复核](DIRECTOR_COST_RECHECK_20260907.md)、[效率与故障优化方案](DIRECTOR_EFFICIENCY_PLAN_20260906.md)、[一次选片时长审计](DIRECTOR_ONCE_DURATION_AUDIT_20260906.md)。历史报告中的运行状态、旧上限和完成声明仅按其当时证据理解。
- 当前可执行库存离线重建为171条，通过1–8秒范围得到169条，与该次商品审计记录的可执行短句数一致。未重新调用模型验证语义结果。
- 本轮只审计并生成报告、证据文件，没有修改业务代码、重启服务、改写片单、重新导出或发起付费AI请求。
