# AYOBE 开场与短句保真修复 / 死机续接断点

## 当前结论（不可省略）

**程序保真修复已落地；整片语义质量验收仍未通过。不是发布通过记录。**

用户批准修复：保住 AI 选定的短句连接，明确主商品范围，在现有第二次 AI 内比较开场组合。保持正常链路两次 AI、1–5 秒短句优先（原有 5–8 秒完整句例外）、逐词编辑和旧成片入口，不改 M3，不自动换开头，不用程序代选语义。

2026-09-03 用户再次反馈死机。已重新核对源码修改、两轮真实请求、预览和导出输入报告均仍在磁盘。不要因为对话中断重做付费测试。

## 已落地修改

- `app/commercial_analyzer.py`：素材标题只作商品身份提示，显式主商品优先；Story 返回 `product_scope`，不默认将衬衫和同场下装合成套装。该意图保留到 Casting 和最终导演合同，不改 M1 发现架构。
- 在现有 Casting 调用内比较真实开场组合，记录 `opening_selection`；程序核对其是否与实际首章一致，不替 AI 选择。
- 真实第一轮暴露“反问→反问”被误当兑现后，加强了现有 Casting 的连读要求，并要求输出已有字段 `chapter_readthrough`。原话仍由字幕 ID 重建，模型写的文本不能替代源字幕。
- 记录 `continuity_links`；依赖顺序错误、重复字幕 ID、AI 连读文字与源字幕不一致，仅出非阻断警告，不自动删改或重排。
- `web_client/server.py`：导演短句不再因以“因为/而且/然后/就是”等开头而自动取消勾选；不再用单句独立性规则剥掉导演序列中的承接句。原有非导演分支保持兼容。
- 保留用户逐词删除；违禁词和本次内容边界仍有效。智能成片与混剪最终过滤统一沿用本次任务的内容政策，避免前面允许、导出又用另一套政策删除。
- 预览和导出记录安全边界造成的实际删减，开场受影响时在工作台提醒，不静默宣称原故事完整。
- `app/single_pass_director.py` 只传递 `product_scope/opening_selection` 元数据，未改 M3、时长、物化、剪辑执行。
- 预览缓存签名加入 `opening-fidelity-v1`，重启后新任务不误复用旧选片缓存；未删除旧缓存。

## 已完成验证

- 最终 187 项定向测试通过（商业导演、预览入口、逐词编辑、single-pass 合同、前端参数、偏好、设置、既有执行入口，以及新增 16 项开场保真测试）。
- 内容边界另用隔离临时配置运行，17 项通过。合计 **204 项**，不是 204 次真实模型测试。
- Node 语法检查、已跟踪修改的 `git diff --check` 通过。
- 旧 AYOBE 已选片单回放：旧预览规则保留 18 项，保真模式保留 21 项（此数为预览→精确子句输入，不能混同旧 MP4 时长）。
- 已知短句测试 213/214/215/226/227/235/236，7 条均可进入精确剪辑输入。“因为”不会再被程序单独丢掉。这组只用于回放验证，没有指定给新 AI，不能作为 AI 自主选片成功证据。
- 两轮真实回归均调用正式 `_run_commerce_director_preview` worker，复用真实 SRT/words；未伪造选择，未重跑 ASR，未执行 M3，未生成新 MP4。

## 真实回归：两轮均保留，不挑成功样本

| 轮次 | AI 调用 | 时长（预览/导出输入） | worker 用时 | 总 tokens | 结论 |
|---|---:|---:|---:|---:|---|
| 17:21 第一轮 | 2 | 47.37s / 47.37s，15 个短片段 | 63.33s | 25,509 | 自主选择好 Hook 213，但未正确兑现，仍有残句 |
| 17:26 加强连读后 | 2 | 37.56s / 37.56s，12 个短片段 | 73.06s | 26,300 | 再次选 213，但漏掉 214，仍有残句和 215 重复 |

实际 ledger 模型两轮均为 `deepseek-v4-pro`，配置界面原值为 flash，经现有导演模型解析规则映射；本轮未更改模型或 thinking 开关。两轮合计 4 次请求、51,809 tokens、0 自动重试。正常用户链路仍是每方案 2 次，不是增加到 4 次。

### 第一轮

核心商品已明确为单件莱赛尔抽褶衬衫。最终 AI 原始顺序：

`213 → 230 → 106 | 255 → 293 → 210 | 375 → 380 → 626 | 121 → 273 → 924 | 150 → 153 → 154`

- 213：“你家里有那么多白衬衫，为什么你唯独想选我这件有原因的？”
- 230：“这个形状是。正常的白衬衫能做到的吗？”
- AI 把两个反问称为问答兑现，实际并未给出明确答案。
- 375：“看到没？你要超说了，不要露肚子来。”仍是异常口播。
- 150：“上衣的话，里面有21%的人棉加百分。”缺少成分下半句，后面接防皱不能补完数字。

### 第二轮

标题为“普通白衬衫没有的抽褶造型感，穿出清冷高智感”。最终 AI 原始顺序：

`213 → 215 | 17 → 159 → 210 | 766 → 237 | 528 → 553 → 952 | 95 → 215`

- 213 后直接跳到 215“给不到你的那种干净利落的细节精细化的裁剪”，漏了“因为这件衣服……普通白衬衫”这一必要承接。仍误判为完整兑现。
- 237 尾部“是高”、528 尾部“这样子的小”、952“……看一下那张”、95“……很难坐在”均有明显残缺。
- 215 重复一次。代码没有自动删除或换句，忠实留下了错误，供人工编辑。
- AI 自述约 38.64s，程序按源区间实算为 37.56s。AI 的自报时长、自评 complete/pass 不是客观质量验收。

**不得把这两轮描述为“好片已修好”。新增连读字段也没有在此次样本中证明能解决语义质量；需要保留该负结果。**

## 文件与复现

- 原素材：`C:\工作\小贤\单品素材\【AYOBE】小贤 8月28日10 00新品 莱赛尔韩系抽褶优雅气质风衬衫 8.28.mp4`，同名 `.srt` 和 `.words.json`。
- 旧真实成片：`C:\工作\output\xx\9.3\AYOBE_小贤 8月28日10 00新品 莱赛尔韩系抽褶优雅气质风衬衫 8_28_20260903_145351.mp4`。不要与新回归输入混用。
- 原只读审计：`C:\工作\output\xx\9.3\edit\ayobe_145351_audit\audit.md`。
- 验证脚本：`workspace/ayobe_opening_fidelity/verify_ayobe.py`。无参数只回放；`--live` 才会付费跑新方案。请勿因恢复任务自动重复加 `--live`。
- 第一轮：`workspace/ayobe_opening_fidelity/20260903_172136/live_report.json`、`preview.json`。
- 第二轮：`workspace/ayobe_opening_fidelity/20260903_172637/live_report.json`、`preview.json`。
- 原始两次 AI 返回、M2、费用与参数分别在：
  - `workspace/ui_commerce_director_experiment/ayobe-opening-fidelity-20260903_172136/`
  - `workspace/ui_commerce_director_experiment/ayobe-opening-fidelity-20260903_172637/`
- 第一轮回归脚本用 `director_candidate_id` 读取元数据，旧投影实际键为 `candidate_id`，所以首轮 `live_report.json` 的 ID 列为空；不能理解为丢失血缘。`preview.json` 和原始 Casting 中有完整 ID。已给生产投影补同义字段，并修正脚本；不重写原回归报告。

## 运行状态与续接边界

- 源码在 `C:\Users\周美彤\Documents\GitHub\LiveClipper`，不是入口目录副本。
- 死机前 8765 服务为 16:25 启动的 PID 788，未加载本轮后端修改。用户本次反馈死机后重新核对，8765 已不可连接、未见监听，当前源码服务没有运行。此前自动停止进程曾被环境策略拦截，本轮没有换手段强行停止；后续需重新启动源码服务，不能只刷新页面。
- 未打包、未发布、未提交 git、未更改 API Key 或用户设置、未清理素材/缓存。
- `_load_keyword_config` 曾提示用户词库不可用并回退默认词库；没有擅自修复或覆盖词库。两轮按当前实际加载配置跑，不能假定与旧视频的所有设置完全一致。
- 余下问题已定位到 Casting 的语义选择/连读判断：模型能找到 213，却不会稳定带齐必要承接、剔除真正残句。不得让 M3、自动换 Opening、程序语义删句来掩盖。
- 后续若要继续试验，应先确定一个能直接检验“问句→真实回答→证据”的小型验收，而不是再扩章节、追时长或无止境堆 Prompt。当前任务的整体质量目标仍未达成。
