# 2026-09-03 AI 导演参数收口与接入

## 任务范围

用户已同意执行：修复设置页内容边界溢出；收口智能成片/混剪的 AI 参数；把现有参数和长期选片倾向接入当前两次 AI 导演链。不得增加 AI 调用，不修改 M3，不改选句架构，不打包或发布。

## 已完成并验证

- 智能成片、混剪统一显示「AI导演参数」。合并「本次主方向 / 成片取向」为「导演方向」，移除可见严格度，质量底线不再由宽松选项降低。
- 「本次覆盖卖点」改为「本次优先讲」；重点词单独传递，修复输入框被挤进标签列的问题。
- 新增 director_controls：类目、主商品、导演方向、优先讲、重点词、长期选片倾向、避选、开头和收尾偏好。
- source_info.json 和 director_controls.json 记录实际参数，并传入 Story Director 和 Beat Casting；备选方案重跑继续沿用原参数。修复结果写入时覆盖原始参数快照的问题。
- 偏好只帮助 AI 选择，不允许程序自动插句，也不能突破内容边界和素材事实。
- 内容边界卡片下拉宽度与网格列宽统一，避免向右溢出。

## 验收结果

- 171 项定向测试通过：商业导演 62、导演预览入口 34、偏好方向 8、设置自动保存 1、现有执行入口 3、内容边界 17、逐词编辑 43、参数前端合同 3。
- 内容边界测试使用隔离的临时设置运行，避免真实用户已保存配置影响默认值断言；没有修改用户设置。
- 前端脚本、Python 语法检查通过；git diff --check 通过。
- 1280×720 实际页面验证：智能成片和混剪重点词输入框正常；8 个内容边界下拉框均未溢出；旧场景搭配偏好迁移为可见优先项；无页面控制台错误。
- 源码服务地址为 http://127.0.0.1:8765/ ，运行目录已核对为当前仓库。最后自动重启被环境策略拦截，原进程仍在运行；需要手动关闭并重新启动源码服务，才会加载最终后端改动。
- 没有消耗付费 AI；没有新增 AI 请求；没有改 M3、打包、上传或发布。回归测试验证参数确实进入两次请求，不代表已验证新一轮真实模型成片质量。

## 主要代码位置

- app/commercial_analyzer.py：统一 director_controls 并接入 Story Director / Beat Casting。
- run_m3_new_golden_plan_fidelity.py：现有执行入口传递与记录参数，不改物化合同。
- web_client/server.py：预览、备选方案复用、审计快照持久化。
- web_client/frontend/index.html、assets/app.js、assets/styles.css：界面收口、收集发送、旧预设兼容和布局修复。
- app/ai_clipper.py：旧链路兼容新的独立重点词字段。

## 下一步人工验证

重新启动源码服务并刷新页面后，新建一次预览测试导演方向、优先讲及重点词；新参数不会回写或改变已生成的旧方案。备选方案仍需用户确认后才重新调用 AI。

此文件是本地开发进度记录，不代表 Codex 对话记录已恢复。源码保存在当前仓库，不依赖对话列表显示。
