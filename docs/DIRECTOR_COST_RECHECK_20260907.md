# 2026-09-07 单方案费用复核

本次 `smart-cut-1788721263-9c2af2` 预览的 48,495 tokens、谷时约 ¥0.175 与用量账本一致。提示词压缩已启用，但没有证明总费用下降。

## 实测

- 素材：`Ayobe小贤202609060951_part_002.mp4`；60 秒、单方案、1.15 倍速。
- 两次成功调用，零重试、零第三次 AI。两次开始于北京时间 03:01，属于谷时。
- 故事调用：输入 7,279，输出 1,895，耗时 13.78 秒，估算 ¥0.018518。
- 选片调用：输入 6,859，输出 32,462，其中推理 30,231、正文 2,231；耗时 242.67 秒，估算 ¥0.1563675。
- 合计输入 14,138（缓存命中 640）、输出 34,357；估算 ¥0.1748855。推理部分估算 ¥0.1360395，占总费用约 78%。故事调用未返回推理用量明细，不能把其缺失值解释为已观测到零。
- 选片 `reasoning_effort=low`，`max_tokens=32768`，实际输出 32,462，`finish_reason=stop`。正常完成，但接近上限。
- 运行请求的 system 指纹与当前压缩版完全相同：`5a24fed0575602ae60b7da8e0c079e7dff372b35b286d94020d18f5ab01934c5`。
- 最终 25 句，原声 62.296 秒，预计成片 54.17 秒；落在当前 50–70 秒容差内，低于 60 秒中心目标。

官方价格于本次核对：缓存输入 ¥0.05/百万、未缓存输入 ¥1.5/百万、输出 ¥4.5/百万。来源：https://api-docs.deepseek.com/zh-cn/quick_start/pricing/

官方思考模式文档确认 `low` 是有效推理等级，并支持显式关闭思考；低等级不是固定推理 token 预算。来源：https://api-docs.deepseek.com/zh-cn/guides/thinking_mode/

## 发现与处理

1. **已修正预算换算退步。** 上轮压缩时漏接已有的按比例预算换算。本次第一遍章节原声预算合计 61 秒，但 60 秒 / 1.15 倍速需要选 69 秒原声。现在紧凑执行合同直接携带程序换算后的各章预算，合计 69 秒。只做数字换算，保留原故事、顺序和章节职责；不代选句、不补片、不新增 AI 调用。
2. **故事与禁项冲突仍待修复。** 本次内容合同禁止价格和售后，第一遍仍生成以价格猜测、99 元比较为必讲要求的 C1/C2，C7 还提及售后承诺。最终文本存在“它值多少钱”等内容。第二遍同时接到冻结故事和禁项，存在明显执行冲突；这可能增加协调负担，但当前证据不能量化它造成了多少推理 token。
3. **成本主项是选片推理。** 仅删除输入说明或返回正文，无法保证抑制模型内部推理。继续降低上限有截断完整 JSON 的风险。下一步应先消除故事与禁项冲突，再以同一素材、同一冻结故事比较低推理与非思考选片的费用、时长、口播闭合及商品归属。

前一条 60 秒单方案使用 `part_001`，本次使用 `part_002`，不是严格同素材 A/B；不能把两次用量差异全部归因于代码改动。

## 验证边界

- 本轮相关 150 项离线回归通过，语法与差异检查通过。
- 以本次已保存故事离线验证，执行预算由 61 换算为 69 秒。
- 本轮未调用 DeepSeek，未产生新的模型费用。
- 自动审批策略拒绝停止并重启源码服务；复核 PID 32164 仍在运行，因此预算修正尚未加载到当前进程。工具只给出 blocked by policy，没有具体原因。
- 预算修正后的实际成片、费用变化及禁项修复均未完成真实验收，不得据离线测试宣称降本成功。

原始证据：`workspace/ui_commerce_director_experiment/smart-cut-1788721263-9c2af2/ai_cost_report.json`、`source_info.json`、`*.director_story.response.txt`、`*.director_duration_control.response.txt`；全局用量账本 `%APPDATA%/LiveClipper/ai_cost_ledger.jsonl`。
