# LiveClipper V4 2026.9.6.1

状态：业务 ZIP 已上传 OSS 并完成公网回下载；候选通道保持 `hold`，等待最后发布 `ready`。

## 版本与范围

- 源码提交：`9c4a8f73ba48cfe5266e15a0b612050664eff93c`。
- 发布类型：`runtime_v4_business`；Core 仍为 `4.0.0`。
- 包含：AI 导演主商品范围校验、目标时长与章节完成度核算、多套完整方案预览和批量选择、响应压缩与解析修复、按需费用明细、ASR 会话复用、预览词句身份稳定，以及排期切割边界修复。
- 不包含：launcher、Host、更新器、公钥、原生/ML 依赖、用户 workspace、实验脚本、素材、缓存、旧候选和救援工具。

## 构建与验证

- 从同一干净提交连续构建两次，ZIP 字节一致。
- 业务 ZIP：`release_artifacts/v4_2026.9.6.1/LiveClipperBusiness_2026.9.6.1.zip`。
- 大小：`1542290` 字节；业务文件数：`87`。
- SHA-256：`a1d2f3befa99f4105e3fa71f025baaf416de372e82bff837704df7d1e6d2f7fd`。
- manifest SHA-256：`e32875d8af116b9c123d4d752323c4ac537c45a619e984a6d97df8438f0b23f4`。
- Ed25519 key id：`1905329f73f719d3`；签名、完整文件集、入口点和 Core 兼容性通过。
- Python 编译和前端 JavaScript 语法通过。
- 完整测试：`1133` 项通过，`2` 项跳过。
- 干净提交内的商业导演打包/商品范围/响应结构/时长控制回归：`55` 项通过。

## 公网与升级闭环

- OSS 地址：`https://lc-update.oss-cn-beijing.aliyuncs.com/liveclipper/v4/LiveClipperBusiness_2026.9.6.1.zip`。
- 公网回下载大小、SHA-256、签名、manifest 和 Core 兼容性与本地包一致。
- 隔离执行 `2026.9.4.1 -> 2026.9.6.1`：公网下载、签名校验、原子启用、旧版本回退项保留、包内服务端和新增模块导入、包外用户工作区、用户数据保持均通过。
- 自动回滚、坏签名、坏哈希、断点续传和事务失败路径由完整测试套件覆盖；本次未改 Core 或更新器。

## 未运行与既有边界

- 未在第二台干净电脑做 GUI 人工验收；未在本次发布流程中重新消耗付费模型做真实素材效果评价。
- 历史交付 Core 的完整文件校验问题仍未解决；本次包不包含、不修改也不重新签署 Core。这里的通过仅指业务包、业务更新事务与包内运行入口。
- 已污染旧版本的专用救援工具仍不随本业务包发布；无法启动的软件需先恢复到可运行状态，才能使用应用内更新。
